pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()

        // Detailed Story
        maven { url 'https://jitpack.io' }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()

        // Detailed Story
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "BoTo Travel"
include ':app'
